#pragma once

#include "Asset/Core/AssetCommonTypes.h"

namespace Asset
{

    enum class EPixelFormat : uint8
    {
        Unknown = 0,
        R8,
        RG8,
        RGB8,
        RGBA8,
    };

} // namespace Asset
